# AGENTS.md

## 项目定位

- 本项目用于物理学习、概念理解、学习路线执行和科研入门。
- 核心目标是通过讲解、推导、习题、数值模型和方向试学，形成可验证的学习成果。
- 回答应面向实际问题，避免只罗列术语、课程或研究方向。

## 协作与表达

- 默认使用简体中文；关键课程、论文和前沿资料可以引用英文一手来源。
- 专业术语首次出现时给出直观解释，并明确区分已核实事实、推断和建议。
- 物理讲解优先采用“具体场景 → 研究问题 → 常用方法 → 所需基础”的顺序。
- 给出公式时说明变量含义、单位、适用条件和近似；计算时检查量纲、数量级和边界情况。

## 开始一次学习

- 生成学习内容前，依次读取 `state/learner-profile.md`、`state/current.md` 和当前阶段对应的路线文件；不要一次加载全部路线。
- 用户没有指定时长时，按约 90 分钟设计一份学习单。
- 新学习单放在 `sessions/YYYY/MM/YYYY-MM-DD-<topic>.md`，从 `templates/learning-session.md` 创建。
- 学习单应包含目标、前置检查、讲解、练习、用户结果、AI 批阅、下一步和相关项目链接。
- 同一学习过程只维护一份学习单，不另建重复的日报、周报或总结。

## 练习与批阅

- 学习者负责练习中的计算、推导和作答；不要预先把完整答案写进练习。
- 可以使用不同于练习题的示例解释方法，但不能借示例直接泄露练习答案。
- 用户卡住或作答错误时，按“指出问题 → 最小提示 → 方法框架 → 完整解法”逐级介入。
- 完整解法仅在用户明确要求时给出；经过两次尝试仍未解决时，可以主动询问是否需要完整解法，但不要直接展示。
- 批阅应检查推理、公式、单位、数量级、代码或数值结果、物理解释和模型适用条件。
- 批阅时可以独立计算或运行代码核验结果，但不得把代理的计算冒充为学习者成果。

## 进度与学习证据

- `state/current.md` 是唯一的跨学习单进度来源，只记录当前阶段、已验证成果、待解决问题和下一步。
- 学习单状态只使用 `planned`、`submitted`、`reviewed`。
- 只有学习者提交了可验证的讲解、推导、习题或项目成果，才能将相应能力记为已验证。
- 自述基础、阅读完成和观看课程不能单独作为“已掌握”的证据。
- 批阅完成后更新同一学习单；仅在跨学习单状态发生变化时同步更新 `state/current.md`。
- 程序、图像和较完整的实验成果放在 `projects/<project-name>/`，学习单只链接这些成果。

## 路线推进

- 路线中的周数是建议节奏，阶段过关条件优先于日期；未达到条件时应补缺或顺延。
- 不默认学习者要同时深入三个方向。完成三个方向试学后，再确定一个主方向和一个辅助方向。
- Python 用于物理建模、数值求解、绘图和验证，不安排通用 Python 语法课程。

## 科研调研与来源

- 论文、实验结果、课题组方向、招生信息和研究热点必须有可追溯依据，不得虚构。
- 对可能变化的信息进行联网核验，在结论附近标注来源和核验时间。
- 优先使用论文原文、期刊或会议页面、科研机构及高校官方页面等一手来源。
- 来源存在冲突时，说明冲突和判断依据，不强行合并为单一结论。

## 文档治理

- 创建、拆分、重组或审核 Markdown 文档时，使用 `$document-governance` 执行按需规则和检查流程。
- 根 `AGENTS.md` 只保留全项目必须生效的规则；安全边界、学习证据规则和跨目录不变量不得只写在子目录。

## 项目技能

- `.agents/skills/` 中的项目技能必须使用简体中文编写，包括 YAML 中面向代理或用户的描述、`SKILL.md` 正文、参考资料和 UI 展示文案。
- 每个项目技能都必须包含 `agents/openai.yaml`，其中至少提供与技能一致的中文 `display_name`、`short_description` 和 `default_prompt`；默认提示必须使用准确的 `$skill-name` 调用标记。
- 技能机器标识、目录名、调用标记、命令和代码标识符保留规范要求的英文形式；新建或更新技能时不得为追求中文化而破坏这些稳定标识。

## 自动提交

- 本项目明确授权代理自行判断并执行 `git commit`，满足以下条件时无需逐次确认；该授权不包含 `git push`。
- 完成一个独立、可验证、可回滚的工作单元后，使用 `$git-learning-checkpoint` 审核并提交。
- 适合自动提交的检查点包括：学习单完成批阅、路线或当前状态形成完整调整、项目成果验证通过、项目配置形成一致变更。
- 学习单仍为 `planned` 或 `submitted`、工作尚未完成、验证失败、存在敏感信息，或改动混入无法可靠区分的无关内容时，不得提交。
- 工作区存在用户的其他改动时，只暂存当前任务文件；文件边界或改动归属不清时停止提交并说明原因。
- 自动提交必须采用 Conventional Commit 格式，提交后报告提交哈希、消息和剩余未提交改动。

## 版本管理

- 保持改动最小且聚焦当前学习任务，不修改无关文件。
- 不执行 `git push`、发布或删除性操作，除非用户另行明确授权。
- 不把虚拟环境、缓存或编辑器临时文件加入版本管理。
